---
title: Catchball API
description: Bidirectional priority alignment. Proposals, loops, threads, acceptance, rejection.
sidebar_label: Catchball API
sidebar_position: 6
audience: Developers
status: published
---

import DocsSystemMapDiagram from '@site/src/diagram-kit/DocsSystemMapDiagram';
import DocsCatchballLoopDiagram from '@site/src/diagram-kit/DocsCatchballLoopDiagram';

# Catchball

Catchball is the bidirectional alignment mechanism. Teams submit proposals; authorities review, accept, return, or reject. Every exchange is recorded.

See [Concepts: Catchball and authority](/concepts/catchball-and-authority) for the full model.

All Catchball state changes route through RPCs. Direct table writes for proposals or loop states are rejected.

<details className="margin-bottom--md">
  <summary>Functional map — where Catchball sits</summary>
  <DocsSystemMapDiagram />
</details>

<details className="margin-bottom--md">
  <summary>Loop choreography — one iteration</summary>
  <DocsCatchballLoopDiagram />
</details>

---

## List loops

```
GET /v1/catchball/loops
```

**Scopes:** `catchball:read`

**Query parameters:**

| Name | Type | Description |
|------|------|-------------|
| `cycle_id` | uuid | Filter by cycle |

---

## Get a loop

```
GET /v1/catchball/loops/:id
```

**Scopes:** `catchball:read`

---

## List proposals

```
GET /v1/catchball/proposals
```

**Scopes:** `catchball:read`

**Query parameters:**

| Name | Type | Description |
|------|------|-------------|
| `cycle_id` | uuid | Filter by cycle |
| `loop_id` | uuid | Filter by loop |
| `status` | string | Filter by proposal status |
| `submitter_id` | uuid | Filter by proposing user (`proposed_by`; `proposer_id` alias supported) |
| `page`, `per_page` | integer | Pagination (same envelope as other list endpoints) |

---

## Submit a proposal

```
POST /v1/catchball/proposals
```

**Scopes:** `catchball:write`

Routes through `catchball_submit_proposal` RPC. This endpoint submits an **existing** `catchball_proposals` row (draft packet formed in-product or via the same workspace); it does **not** create new rows without a proposal id.

**Body:**

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `proposal_id` | uuid | **required** | Existing proposal row (`packet_id` accepted as synonym) |
| `loop_id` | uuid | **required** | Must match the proposal’s `loop_id` |
| `submitter_id` | uuid | **required** | Actor performing the RPC handoff (`proposer_id` accepted as synonym) |
| `rationale` | string | | Passed as `p_rationale` when non-empty |
| `tradeoffs` | jsonb | | Optional structured payload (`p_tradeoffs`) |
| `content` | object | | Convenience: merged payload when `tradeoffs` omitted — top-level keys like `ranked_items` map to RPC `p_tradeoffs`; `content.rationale` may supply rationale |

**Proposal content:**

```json
{
  "rationale": "Based on Q2 customer feedback and engineering capacity...",
  "ranked_items": [
    {
      "item_id": "item-uuid-1",
      "rank": 1,
      "rationale": "Highest customer impact, clear path to execution"
    },
    {
      "item_id": "item-uuid-2",
      "rank": 2,
      "rationale": "Required for regulatory compliance in Q3"
    }
  ],
  "constraints": [
    "Engineering capacity limited to 4 team-weeks in Q3",
    "Item 3 has a hard dependency on infrastructure work not in this pool"
  ]
}
```

**Request:**

```bash
curl -X POST "$PAI_BASE/catchball/proposals" \
  -H "Authorization: Bearer $PAI_KEY" \
  -H "Content-Type: application/json" \
  -d '{
    "proposal_id": "proposal-uuid",
    "loop_id": "loop-uuid",
    "submitter_id": "team-lead-user-uuid",
    "content": { ... }
  }'
```

---

## Get a proposal

```
GET /v1/catchball/proposals/:id
```

**Scopes:** `catchball:read`

Returns the proposal and its full revision history.

---

## Accept a proposal

```
POST /v1/catchball/proposals/:id/accept
```

**Scopes:** `catchball:write`

Routes through `catchball_accept_packet` RPC. The actor must hold the decision authority for the target loop.

**Body:**

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `actor_id` | uuid | no | Defaults to authenticated context when omitted |
| `note` | string | no | Recorded on the governance path |

---

## Reject a proposal

```
POST /v1/catchball/proposals/:id/reject
```

**Scopes:** `catchball:write`

Routes through `catchball_reject_proposal` RPC. Atomically updates proposal status and appends a `proposal.rejected` governance event.

**Body:**

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `actor_id` | uuid | no | Authority rejecting |
| `note` | string | no | Reason for rejection — recorded where supported |

---

## Return a proposal

```
POST /v1/catchball/proposals/:id/return
```

**Scopes:** `catchball:write`

Routes through `catchball_return_proposal` RPC. Atomically updates proposal status to `returned` where applicable.

**Body:**

| Field | Type | Required | Description |
|-------|------|----------|-------------|
| `actor_id` | uuid | no | Actor returning |
| `note` | string | no | Guidance for revising |

---

## Get proposal thread

```
GET /v1/catchball/threads?proposal_id=:id
```

**Scopes:** `catchball:read`

Returns the discussion thread (structured comments) on a proposal — the exchange between the submitting team and the reviewing authority.

---

## What's next

- [Concepts: Catchball and authority](/concepts/catchball-and-authority)
- [Guide: run a headless session](/guides/run-headless-session)
